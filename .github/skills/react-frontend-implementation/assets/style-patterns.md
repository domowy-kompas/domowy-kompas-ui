# Style patterns

- Follow the existing project styling system first.
- Keep selectors local to the component when possible.
- Prefer descriptive class names that map to the component structure.
- Reserve global selectors for layout or theme primitives.
- Keep focus states visible and distinct.
- Use responsive spacing and layout rules instead of hard-coded widths.
- Keep visual changes small when a feature is primarily functional.